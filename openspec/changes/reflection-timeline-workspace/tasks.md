## 1. 参照実装とユーザーフロー（先行）

- [x] 1.1 `ユーザーフロー.md` を companion として作成し、統合ワークスペースの画面遷移（振り返りを書く→ビューをめくる→明日の計画で登録）を題材付きで歩く（唯一の受け入れ基準）
- [ ] 1.2 `ref/reflection/` に統合後 HTML を写経（左メイン＝ビュー切替の横スクロール＋右サイドバー＋日付ストリップ、`rf-*` 配色・寸法の basis）
- [ ] 1.3 既存 `ref/timeline/`・`ref/reflection/振り返り.dc.html` の視覚要件を統合参照へ束ね、差分を明記

## 2. timeline.js のリファクタ（挙動不変）

- [ ] 2.1 `timeline.js` を「対象日を引数に任意コンテナへ描画する `render(container, date)`」＋薄い `show(root)` ラッパへ分離（記録・離席ドラッグ・ポップオーバー・自動スクロールの副作用を保持）
- [ ] 2.2 `hide()`/タイマ後始末が新構造でも正しく働くことを確認
- [ ] 2.3 既存タイムライン系 e2e（`e2e/` の該当）で回帰確認（記録・離席・同時記録の描画不変）

## 3. reflection.js の器化（左メイン＋右サイドバー）

- [ ] 3.1 `show()` を「左メイン（ビュー切替コンテナ）＋右サイドバー」を組む器へ作り替え、`activeDate` を単一所有する ctx を導入
- [ ] 3.2 右サイドバーへ本文エディタ（`md-editor.js`）・5段階気分・目標日記（`journalCorner`）を移設（保存動線 `doSave`/`flush`/`putReflection`/`putGoalJournal` は不変）
- [ ] 3.3 右レール「過去の振り返り一覧」を撤去（役割は日付ストリップへ移管）
- [ ] 3.4 簡略リボン（`renderTimelinePanel`/`buildRibbon`/オーバーレイパネル）を撤去

## 4. 左メインのビュー切替（横スクロール・デッキ）

- [ ] 4.1 `overflow-x:auto; scroll-snap-type:x mandatory` のデッキ＋各ビュー `scroll-snap-align`＋内部縦スクロールを実装（`rf-*` クラス・CSSOM のみ、インライン style 禁止）
- [ ] 4.2 ビュータブ／‹ ›／現在ビュー判定（スクロール位置逆算）を実装
- [ ] 4.3 ビュー①「タイムライン（完全版）」に `timeline.render(container, activeDate)` をマウント
- [ ] 4.4 ビュー②「一日の配分」を独立全幅ビュー化（`renderAlloc` を移設。エディタ上部常設バーは廃止。降順＋未記録最下部・空状態を維持）
- [ ] 4.5 ビュー切替が `activeDate` を変えないこと・現在ビューのみ更新することを担保

## 5. 日付ストリップ（対象日ナビゲーション）

- [ ] 5.1 前後日チップ（曜日・日番号）＋気分色ドット（`GET /api/reflections` の satisfaction 由来）＋未来日の視覚区別を実装
- [ ] 5.2 ‹ ›・チップ直接選択・「今日」復帰を実装し `activeDate` を更新
- [ ] 5.3 `activeDate` 変更で現在ビュー＋右サイドバー（本文・気分・目標日記）を同期再描画
- [ ] 5.4 任意日ジャンプ用の日付ピッカーを併設（従来 `<input type=date>` の役割を吸収）

## 6. 明日の計画ビュー（tomorrow-plan-capture）

- [ ] 6.1 本文の明日段落から候補チップを抽出するヒューリスティック（句読点・接続詞で分割、最大数・短句化）を実装
- [ ] 6.2 候補チップのワンタップ登録＋「登録済み」表示（二重登録防止）
- [ ] 6.3 大きな入力欄によるクイック登録（Enter 連続、IME 変換確定 Enter を除外＝`enter-submit-ime-guard` と整合）
- [ ] 6.4 登録＝`createTask({status:'TODO', due:翌日})`（`computeDue` 相当）で本物のカンバンへ。カンバンの「明日のタスク n/N」と同集合になることを確認
- [ ] 6.5 登録済みタスクの余白ある縦リスト表示・ドラッグ並べ替え（`kanban-task-reorder` の within-column 順序と整合）・取り消し
- [ ] 6.6 旧「振り返りを終えて明日の計画へ →」カンバン遷移動線を撤去（振り返りタブ内で完結）

## 7. ナビゲーション統合とディープリンク

- [ ] 7.1 `index.html` のタブ定義から「タイムライン」を除去（`今日 / 振り返り / カンバン / 目標 / 設定`）
- [ ] 7.2 `main.js` の `SCREENS` から `timeline` を除去し、`#timeline` ディープリンクを「振り返り＋タイムラインビュー選択」へ移行（通知経路のリンク切れ無し）

## 8. デモ・検証・仕上げ

- [ ] 8.1 デモモード（`showDemo`）を新レイアウトへ追従（閲覧専用・明日の計画の作成導線は非表示）
- [ ] 8.2 `demo-seed` の固定 day_key で配分・タイムライン・振り返り閲覧が新レイアウトで再現することを確認（`POST /api/demo/reset` 経路）
- [ ] 8.3 スクショで `ref/reflection/` 統合参照との一致を検証（別物事故の再発防止）
- [ ] 8.4 明日タスク登録の end-to-end（振り返り本文→候補タップ/入力→カンバンに due=翌日 で出現）を e2e で確認
- [ ] 8.5 CSP 適合（インライン `style=` 不在）とキーボード（IME・ショートカット）回帰を確認
