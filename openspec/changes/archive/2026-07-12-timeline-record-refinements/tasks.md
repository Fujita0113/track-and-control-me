## 1. DB / 移行

- [x] 1.1 `activity_log_entry` に `co_record_group_id INTEGER`（nullable, 既定 NULL）を追加する加算的移行を追記する（バックフィル不要）
- [x] 1.2 移行後に既存の単独 MANUAL 記録が `co_record_group_id=NULL`・`n=1` のままであることを確認する

## 2. サーバー: 同時記録の作成

- [x] 2.1 `addManualEntry` を拡張し、複数カテゴリを1トランザクションで同一 `co_record_group_id`・`n=構成数` の N 行として作成する関数を追加する（単独時は従来どおり `co_record_group_id=NULL`・`n=1`）
- [x] 2.2 重複カテゴリ名・空白のみカテゴリを確定前に正規化・除外する
- [x] 2.3 `POST /api/timeline/:date/manual` を複数カテゴリ配列に対応させる（単一 body は後方互換で受理）
- [x] 2.4 同時記録メンバー削除時に、残メンバーの `n` を新しい構成数へ更新して再按分する

## 3. サーバー: 按分計上と TIMELINE 評価

- [x] 3.1 `TIMELINE` 評価の合計 SQL を `SUM(end_at - start_at)` から `SUM((end_at - start_at) * 1.0 / n)` へ変更する（`n=1` の既存・単独記録は結果不変）
- [x] 3.2 タイムライン payload の MANUAL エントリに同時記録グループ情報（`coRecordGroupId`／構成数）と持ち分を付与する
- [x] 3.3 `GET /api/goals` の `timeline:*` practice に当日実効の `thresholdSeconds` を付与する（強調バッジ用）

## 4. クライアント: 記録ポップオーバー（複数選択・チップ）

- [x] 4.1 `openDraft` のチップをトグル複数選択（選択集合）に変更し、自由入力の複数追加に対応する
- [x] 4.2 確定時に選択・入力された全カテゴリを一括 API で送信し、成功トーストと再描画を行う
- [x] 4.3 `chipCategories()` を「追跡カテゴリ先頭固定（上限外）＋残りを直近使用順に上限まで＋超過は『もっと見る』展開」の3区分に再構成する

## 5. クライアント: 強調バッジと並列表示

- [x] 5.1 `refreshTrackedCategories` を拡張し、カテゴリ名→最大閾値秒の Map を目標 payload から構築する
- [x] 5.2 手動記録ブロックの強調に `≥N分` 閾値バッジを併記する（並列表示は既存 `layout()` に委ね、必要なら持ち分ラベルを補助表示）
- [x] 5.3 記録ポップオーバーの追跡カテゴリチップに `≥N分` 閾値バッジを併記する
- [x] 5.4 同時記録が区間全体を占める並列列で描画されることを目視確認する（単独記録の見た目は不変）

## 6. テスト / 検証

- [x] 6.1 同時記録作成の原子性（途中失敗で部分作成しない）と正規化（重複・空白）の単体テスト
- [x] 6.2 `TIMELINE` 按分の単体テスト（2時間・2カテゴリで持ち分1時間・閾値到達／単独記録は不変）
- [x] 6.3 削除時の再按分（2→1 で持ち分が区間長へ戻る）の単体テスト
- [x] 6.4 強調バッジが評価・集計・解錠に影響しないこと（表示専用）の確認
- [x] 6.5 手動で記録→並列表示→タイムライン再描画のエンドツーエンド確認（`/verify` 相当）
