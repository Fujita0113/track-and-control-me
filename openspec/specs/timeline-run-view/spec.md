# timeline-run-view Specification

## Purpose

タイムラインの AUTO ブロック描画をラン（表示レイヤーでの断片結合）ベースに刷新し、ガター・ラベル・カラム割当のノイズを除去する。データ層（session / creditedMs / gaps 計算）は変更しない。

## Requirements

### Requirement: AUTO ブロックは記録時点のスナップショット identity 単位で生成する
タイムラインの AUTO ブロック生成（サーバ側の近接セッション結合）は、束ねる単位を各セッションの `stable_group_id` ではなく、**記録時点のスナップショット identity＝（`tab_group_name_snapshot`, `group_color_snapshot`）の組**とする SHALL。同一 identity（名前・色が一致）のセッションのみを近接結合の候補とし、identity が異なるセッションは別ブロックへ分離する SHALL。生成した AUTO ブロックのタイトル・色は、その identity の名前・色とする SHALL。

同一タブグループを改名して使い回した場合（Edge 側で `stable_group_id` は同一のまま名前・色が変わる場合）でも、名前・色が変わった区間は別 AUTO ブロックへ分離し、当時の名前でラベル付けする SHALL。この結合単位の変更は、`session` の生データ、`creditedMs`、`gaps` 計算、`daily_totals_snapshot`、および解錠ルール評価（従来どおり `stable_group_id` 単位を維持）を変更してはならない（MUST NOT）。

これにより、タイムラインの AUTO ブロックは `today-group-breakdown` のグループ別内訳および振り返りリボンと同一の（名前・色）identity 単位で一致 SHALL する。

#### Scenario: 改名して使い回したグループは名前ごとに別ブロックになる
- **WHEN** 同一 `stable_group_id` が、当日 14:15–14:23 は「ブログ投稿」(magenta)、14:23–15:03 は「開発」(blue)、15:08–16:04 は「アルゴリズム」(magenta) として計上されている
- **THEN** タイムラインには「ブログ投稿 14:15–14:23」「開発 14:23–15:03」「アルゴリズム 15:08–16:04」が別々の AUTO ブロックとして描画され、いずれかの名前で全区間を覆う単一ブロックにはならない

#### Scenario: 異なる stable_group_id でも同一 identity なら結合候補になる
- **WHEN** 異なる複数の `stable_group_id` がいずれも記録時点で「振り返り」(purple) として近接して計上されている
- **THEN** それらは1つの「振り返り」(purple) AUTO ブロックへ近接結合される（従来の `stable_group_id` 単位では別ブロックに割れていた）

#### Scenario: 権威データとゲート判定は不変
- **WHEN** 同一のセッション列・同一設定で、AUTO ブロック生成を identity 単位へ切り替える前後を比較する
- **THEN** `daily_totals_snapshot` の per-group 生データ、日の総作業時間、パスワードゲートの解錠ルール評価（`TOTAL_WORK` / `GROUP`）の算出値は変わらない

### Requirement: 同一グループ断片のラン結合表示
タイムラインは、**記録時点のスナップショット identity（`tab_group_name_snapshot` ＋ `group_color_snapshot`）が一致する**隣接 AUTO ブロック a, b を、次の両条件を満たす場合に限り1つのラン（単一ブロック）として描画する SHALL:
1. `b.startAt - a.endAt` が閾値 `away_min_seconds` 未満
2. 区間 `(a.endAt, b.startAt)` に他の描画ブロック（**別 identity の** AUTO、または MANUAL エントリ）が重ならない

グルーピング・列レイアウトのキー、および結合対象の同一性判定はすべて（名前＋色）identity を用いる SHALL。同時オープングループ名（`coactiveGroupKeys` の表示名解決）も identity 単位で解決する SHALL。結合は描画時のみ行い、サーバーの `session` データ・`creditedMs`・`gaps` 計算には影響しない SHALL。

#### Scenario: 微小離席を挟む同一 identity 断片は1ランになる
- **WHEN** 「面接」(pink) の AUTO ブロックが 10:47–10:55 と 11:01–11:22 に存在し、間隔6分が閾値（10分）未満で、間に別 identity のブロックがない
- **THEN** 10:47–11:22 の単一ブロックとして描画され、タイトル「面接」と時間帯ラベルは1回だけ表示される

#### Scenario: 別 identity の作業を挟む場合は結合しない
- **WHEN** 「面接」ブロックの間（5分）に「ブログ投稿」の AUTO ブロックが存在する
- **THEN** 「面接」は2つの独立したブロックとして描画され、「ブログ投稿」の時間がハッチとして描かれることはない

#### Scenario: 名前が変わった同一 stable_group_id は結合されない
- **WHEN** 同一 `stable_group_id` の隣接ブロックが、間隔は閾値未満だが「ブログ投稿」→「開発」と名前が変わっている
- **THEN** 2つは別 identity として扱われ、1つのランへ結合されず、それぞれの名前で別ブロックとして描画される

#### Scenario: 記録済み離席（MANUAL）を挟む場合は結合しない
- **WHEN** 「面接」ブロックの間に MANUAL エントリ「昼食」が存在する
- **THEN** 「面接」は2つの独立したブロックとして描画される

#### Scenario: 閾値以上の間隔では結合しない
- **WHEN** 同一 identity のブロック間隔が23分（閾値10分以上）
- **THEN** 2つの独立したブロックとして描画される

### Requirement: ラン内離席のハッチ表示
ランは内部の間隙（結合で吸収した離席区間）をハッチ（斜線）スライスとして実時間位置に描画する SHALL。スライスの描画高さは最低 4px を保証し、高さ 16px 以上の場合はスライス内に「離席 n分」ラベルを表示、未満の場合はツールチップで時間帯と分数を提示する SHALL。

#### Scenario: 吸収された離席がハッチとして見える
- **WHEN** 10:47–11:22 のランが 10:55–11:01 の間隙を吸収している
- **THEN** ラン内の該当位置（10:55–11:01）にハッチスライスが描画される

#### Scenario: 極小の離席も視認できる
- **WHEN** 吸収された間隙が2分（描画高さ 2.4px 相当）
- **THEN** スライスは最低高さ 4px で描画され、ツールチップで「HH:MM–HH:MM（2分）」を確認できる

### Requirement: ブロックラベルの一回表示と「・同時n」サフィックス廃止
ランおよび単独ブロックのタイトル・時間帯ラベルはブロックにつき1回だけ表示する SHALL。同時オープン数を示す「・同時n」テキストサフィックスは表示しない SHALL。同時性は列分割レイアウト（横並び）自体が伝達する。

#### Scenario: 同時オープン2グループの表示
- **WHEN** 「ブログ投稿」と「振り返り」が同一時間帯に同時オープンだった
- **THEN** 2つのブロックが横並びのカラムで描画され、いずれのタイトルにも「・同時2」は付かない

### Requirement: 詳細ポップオーバーのクレジット内訳表示
AUTO ラン/ブロックのクリックで開く詳細ポップオーバーは、時間帯（スパン）、実働クレジット（結合した断片の `creditedMs` 合計）、同時に開いていたグループ名の一覧（均等割の注記付き）、離席内訳（回数・合計分数・各区間）を表示する SHALL。

#### Scenario: divide-by-N のクレジットが確認できる
- **WHEN** 60分間2グループ同時オープンだったブロックをクリックする
- **THEN** ポップオーバーに実働クレジット約30分と、同時に開いていたグループ名、均等割である旨の注記が表示される

#### Scenario: ランの離席内訳が確認できる
- **WHEN** 2つの間隙を吸収したランをクリックする
- **THEN** ポップオーバーに「離席 2回・合計 n分」と各区間の時間帯が表示される

### Requirement: ガターの正時ラベル限定
時刻ガターは正時（HH:00）ラベルのみを表示する SHALL。ブロック断片境界の時刻ラベルおよびレーンの境界破線は描画しない SHALL。

#### Scenario: 断片が多い日でもガターが整然としている
- **WHEN** 1日に10個以上のブロック断片が存在する日を表示する
- **THEN** ガターには正時ラベルのみが等間隔に表示され、ラベル同士の重なりが発生しない

### Requirement: カラム割当の安定化
列分割レイアウトは決定的である SHALL。クラスタ内のカラム割当では、同一グループが直前のクラスタで使用したカラムが空いていればそのカラムへ再割当し、衝突時のみ first-fit へフォールバックする SHALL。

#### Scenario: 同一グループが連続クラスタで同じ側に留まる
- **WHEN** 「面接」が隣接する2つのクラスタに出現し、直前クラスタで使用したカラムが次クラスタでも空いている
- **THEN** 「面接」は両クラスタで同じカラム位置に描画される

### Requirement: ブロック高に応じたラベルの3段階描画
タイムラインのブロック（ラン・単独・手動記録）のラベルは、ブロックの描画高に応じて3段階で描画し、名前が縦方向に見切れない SHALL。

1. **通常**（十分な高さ）: 名前と時間帯ラベルを表示する SHALL（従前どおり）。
2. **短い**（時間帯まで入らないが名前1行は収まる高さ）: 時間帯ラベルを非表示にし、名前を1行のみ・縦中央寄せ・行間を詰めて表示する SHALL。名前は横方向に収まらない場合のみ省略記号（ellipsis）で丸め、縦方向には決して見切れない SHALL。
3. **極短**（名前1行すら収まらない高さ）: ブロック内にはテキストラベルを描画せず色／パターンのバーのみとする SHALL。名前はブロックのホバー tooltip（`title` 属性相当）で提示し、詳細はクリックで開く詳細ポップオーバーで確認できる SHALL。

いずれの段階でも、ブロック内テキストがブロックの外形からはみ出したり縦に途切れて表示されたりしない SHALL。同時記録で複数カラムが横に並ぶ場合でも、ラベル描画はカラム内に収まり隣接カラムと衝突しない SHALL。

#### Scenario: 通常の高さのブロックは名前と時間帯を表示する
- **WHEN** 十分な高さ（例: 45分・時間帯ラベルが収まる高さ）の「掃除」ブロックを描画する
- **THEN** 名前「掃除」と時間帯「HH:MM – HH:MM」が2行で表示される

#### Scenario: 短いブロックは名前1行のみで縦に見切れない
- **WHEN** 時間帯までは入らないが名前1行は収まる高さ（例: 20分前後）の「掃除」ブロックを描画する
- **THEN** 時間帯ラベルは表示されず、名前「掃除」が1行で縦中央に収まって表示され、文字の上下が途切れない

#### Scenario: 極短のブロックはテキストを描かずホバーで名前が分かる
- **WHEN** 名前1行すら収まらない高さ（最小高さ相当）の「掃除」ブロックを描画する
- **THEN** ブロック内には見切れたテキストが描かれず色バーのみとなり、ブロックにホバーすると tooltip で「掃除」が表示され、クリックすると詳細ポップオーバーで内容を確認できる

#### Scenario: 短いブロックが横に並んでもラベルが衝突しない
- **WHEN** 同時記録で複数の短いブロックが横並びのカラムで描画される
- **THEN** 各ブロックのラベルは自身のカラム内に収まり、隣接カラムへはみ出して重ならない
